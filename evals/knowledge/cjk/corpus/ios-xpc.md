# iOS 服务通信与 XPC

iOS 应用也运行在受限的进程沙箱中。客户端需要后台能力时，可以借助 XPC 连接向另一个进程发送结构化消息；上层框架常把远端接口包装成代理对象，使调用形式接近本地方法。系统守护进程收到请求后仍要核验连接身份与授权范围。

XPC 的消息协议与 Android Binder 并不相同，但两者都面对地址空间隔离、远端失败和同步调用阻塞等问题。接口设计应限制可反序列化的数据类型，并为连接中断准备明确的恢复路径。
