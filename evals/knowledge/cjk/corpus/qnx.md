# QNX 微内核与故障隔离

QNX Neutrino 采用 microkernel 结构，只把调度、线程和消息传递等核心机制留在内核。文件系统、网络栈与多数驱动作为用户态服务运行，客户端通过同步 message passing 请求能力，而不是直接调用庞大的内核子系统。

服务进程拥有独立地址空间。某个驱动崩溃时，故障通常被限制在该进程，可由管理组件重启服务，其他关键任务继续调度。这种故障隔离不会自动保证实时性；工程上仍要分析优先级、阻塞链与最坏响应时间。
