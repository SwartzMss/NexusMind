# TLS 会话密钥计划

TLS 1.3 的 key schedule 使用 HKDF，把握手期间形成的共享秘密逐级派生为客户端与服务端的流量密钥。不同 label 和 transcript hash 提供上下文绑定，使一份根秘密产生用途互相隔离的会话材料。

每个方向采用独立密钥并维护记录序号，AEAD nonce 由固定值与序号组合。序号不得在同一流量密钥下回绕；更新密钥能够缩短单把密钥保护的数据范围，但不能替代对证书和握手消息的验证。
