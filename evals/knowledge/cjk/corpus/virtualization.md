# 虚拟化隔离

虚拟机监控器利用处理器特权级和二阶段地址转换，把来宾操作系统限制在各自的内存映射内。普通来宾不能直接窥探监控器或其他来宾的高权限区域，设备访问则由 IOMMU 与虚拟设备模型约束。

这种方案和 Arm TrustZone 都依赖硬件访问控制，但安全边界不同：虚拟化侧重多个完整操作系统，安全世界则适合较小的可信服务。共享页、虚拟中断和管理接口仍需严格验证，否则隔离配置可能被逻辑缺陷绕过。
