# TrustZone 隔离边界

Arm TrustZone 将处理环境划分为 Secure world 与 Normal world。安全状态会参与总线访问控制，使普通世界的软件不能直接读取只授予安全世界的内存和外设。两侧切换由受控入口完成，安全监控代码负责保存上下文并验证调用参数。

可信执行环境应保持较小攻击面，只承载密钥操作、启动度量等敏感服务。隔离仍依赖正确配置内存控制器、中断路由和共享缓冲区；若边界检查遗漏，硬件分区本身不能阻止逻辑漏洞。
