# 认证加密与密钥派生

AES-GCM 是 AEAD 模式，同时提供机密性与完整性。每次使用同一把密钥加密时都必须采用唯一 nonce；重复 nonce 会破坏认证安全。接收方应先验证 authentication tag，再把明文交给后续解析逻辑，附加数据可以保护未加密的协议头。

系统不应把一个主密钥直接用于所有用途。HKDF 可从输入密钥材料提取并扩展出多个子密钥，通过 salt 和上下文 info 区分协议、方向或会话。密钥派生提供用途分离，但不能补救低熵口令；口令需要专门的慢速 KDF 与随机盐。
