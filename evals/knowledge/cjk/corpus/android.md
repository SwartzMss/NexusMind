# Android 进程通信

Android 把应用放入彼此隔离的 Linux 进程与沙箱。系统服务通常不与客户端共享地址空间，而是通过 Binder IPC 接收请求。Binder 驱动负责跨进程传递事务，代理对象把本地调用编码为 parcel，服务端再解码并执行。

调用方身份会随事务传播，因此服务必须在入口检查 UID 与权限。避免在持锁状态发起同步远程调用，可降低死锁风险；较慢的工作适合交给异步队列。Binder 解决的是进程边界上的通信与身份传递，并不替代业务层授权。
