# Knowledge Runtime API

`KnowledgeCollection.search` returns provenance-resolved chunks. The exact API
name `snapshot()` returns canonical sources and documents without derived index
state. Retrieval evaluation never mutates this collection.
