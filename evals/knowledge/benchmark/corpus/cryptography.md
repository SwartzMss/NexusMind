# Cryptography

AES-GCM provides authenticated encryption when every nonce is unique. HKDF
derives purpose-separated child keys from root key material using salt and info.
Integrity must be verified before plaintext is consumed.
