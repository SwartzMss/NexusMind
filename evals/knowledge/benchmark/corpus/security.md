# Secure Execution

Arm TrustZone separates a secure world from the normal operating system.
Protected services keep authentication secrets outside ordinary applications,
and shared buffers still require strict boundary validation.
