# Unrelated Engineering Notes

## Storage

A page cache can reduce repeated disk reads, but cache locality does not
establish Binder caller identity, QNX pathname ownership, or TrustZone key
isolation.

## Build

Compiler optimization changes generated code layout and does not validate a
certificate chain.
